fields:
  one: !? .props.env
  two: 2
  !if '$_.props.env = test':
    three: 3
    four: 4
  five: 5  

fields:
  one: !? .props.env
  two: 2
  ? !for $.services 
  :
    ? !? $.name 
    : version: $.version
      env: $_.props.env
  five: 5

config:
  image: !? $.name + ':' + $.version
  tag: !? ('v' + $.version)

? !apply '.config'
: name: coder
  version: '1.0'  